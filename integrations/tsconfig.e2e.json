{
  "//": "Module resolution for the per-plugin Playwright specs under integrations/<lane>/e2e/. Referenced by integrations/tooling/playwright.config.ts via its `tsconfig` option; not a build config, and deliberately not extended by any package tsconfig.json here. @playwright/test and @tomic/lib are devDependencies of the `@tomic/e2e` workspace package, so pnpm links them into browser/e2e/node_modules/ — outside the directories node walks up from a spec file in this tree.",
  "compilerOptions": {
    "baseUrl": ".",
    "moduleResolution": "node",
    "lib": ["ES6", "ES7", "ESNext", "DOM"],
    "types": ["node"],
    "strictNullChecks": true,
    "paths": {
      "@playwright/test": ["../browser/e2e/node_modules/@playwright/test"],
      "@tomic/lib": ["../browser/lib/src/index.js"]
    }
  },
  "include": ["*/e2e/*.spec.ts", "../browser/e2e/global.d.ts"]
}
